#HYPER PARAMETRE:

regul = 0.3 #regularization term
alpha = 0.1 #learning rate
nb_iteration = 3000
data_split = (60, 20, 20) #data repartition between training, cross validation and test (in percentage)